# Main Quality Figure

`plot_longbench_ruler.py` consumes the standard LongBench and RULER summaries
produced by `scripts/reproduce_paper_quality.sh` and writes
`longbench_ruler.{png,pdf}`. The LongBench row includes both HSA routing and
the exact Stage-2 routing variants shown in the submitted figure.
